 public boolean isPalindrome(int x) {
        int temp=0;
        temp=x;
        int result=0;
       while(x>0)
		 {
			 int rem=x%10;
			 result=(result*10)+rem;
			 x=x/10;
		 }

		 if(temp==result)
		 {
			  return true;
		 }
        else{
		return false;  
        }
